# Production Environment Configuration
# This file contains safe defaults for Docker deployment

# Database Configuration (automatically configured for Docker)
DATABASE_URL=postgresql://postgres:postgres@db:5432/gamelab

# Application Configuration
NODE_ENV=production
PORT=5000
APP_NAME=Homelab Dashboard
APP_URL=http://localhost:5001
BASE_URL=http://localhost:5001

# Security Configuration (automatically generated on first startup)
SESSION_SECRET=change_this_in_production_use_openssl_rand_base64_32

# Default Admin User (created automatically on first startup)
# You can customize these before first run, or use defaults and change later
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=admin123

# Firebase Configuration (using safe fallback values)
VITE_FIREBASE_API_KEY=demo-api-key-will-use-fallback-auth
VITE_FIREBASE_PROJECT_ID=demo-project
VITE_FIREBASE_APP_ID=demo-app-id

# Email Service Configuration (optional - will use fallback if not configured)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM_EMAIL=
MAILGUN_REGION=us
EMAIL_FROM=noreply@localhost
EMAIL_FROM_NAME=Homelab Dashboard

# SendGrid Alternative (optional)
SENDGRID_API_KEY=

# Game Server Management (optional - will gracefully degrade if not available)
AMP_API_URL=
AMP_API_USERNAME=
AMP_API_PASSWORD=

# Plex Integration (optional)
TAUTULLI_URL=
TAUTULLI_API_KEY=

# HD HomeRun Integration (optional)
HDHOMERUN_URL=

# Instructions for Users:
# 1. Copy this file to .env: cp .env.production .env
# 2. Generate a secure SESSION_SECRET: openssl rand -base64 32
# 3. Update Firebase credentials if using authentication
# 4. Configure optional services as needed
# 5. Run: docker-compose up -d